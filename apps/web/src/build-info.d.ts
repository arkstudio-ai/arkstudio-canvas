// Globals injected by vite.config.ts `define`. Keep this file under
// `src/` so the existing tsconfig `include: ["src"]` picks it up.
//
// Source-of-truth lives in vite.config.ts; updating these values
// elsewhere (e.g. README marketing copy) is fine but won't change what
// the bundle ships.
declare const __ARK_VERSION__: string;
declare const __ARK_REPO_URL__: string;
declare const __ARK_LICENSE_NAME__: string;
declare const __ARK_LICENSE_URL__: string;
