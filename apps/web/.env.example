# Backend base URL for API calls + admin endpoints.
# Defaults to http://localhost:18500 if unset (matches apps/backend default PORT).
# In production, point to your deployed backend or use a reverse proxy.
VITE_API_BASE_URL=http://localhost:18500
